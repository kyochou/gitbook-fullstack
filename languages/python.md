# Python

## Conda

```shell
# 更新到指定版本
conda install -n base conda=23.1.0
# 显示软件的所有版本
conda search python

# 生成新环境
conda create --name paddle python=3.10
# 进入指定环境
conda activate paddle
# 退出当前环境
conda deactivate

```